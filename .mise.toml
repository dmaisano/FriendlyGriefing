# Tool versions for FriendlyGriefing (NeoForge 1.21.1 mod)
# Install: mise install
# Activate: cd into this directory (requires `mise activate` in your shell)

[tools]
java = "temurin-21" # Java 21 toolchain (see common.gradle)
gradle = "9.2.1"   # matches gradle/wrapper/gradle-wrapper.properties
