# Tool versions for FriendlyGriefing (NeoForge 1.20.4 mod)
# Install: mise install
# Activate: cd into this directory (requires `mise activate` in your shell)

[tools]
java = "temurin-17.0.19+10" # Java 17 toolchain (see common.gradle)
gradle = "8.1.1"            # matches gradle/wrapper/gradle-wrapper.properties
